image: node:8.11.1

pages:
 cache:
  paths:
  - node_modules/

 stage: deploy
 script:
 - yarn install
 - yarn build
 - rm -rf public
 - mv build public
 - yarn live-stop
 - yarn live-start
 artifacts:
  paths:
  - public
 only:
 - master
